{
  "name": "environment-compare",
  "version": "1.0.0",
  "description": "Environment screenshot comparison tool",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup": "node scripts/setup.js",
    "generate:images": "node scripts/generate_environment_images.js",
    "generate:html": "node scripts/generate_comparison_html.js",
    "build-and-deploy": "npm run generate:images && npm run generate:html && npm run deploy"
  },
  "dependencies": {
    "puppeteer": "^24.22.3"
  },
  "devDependencies": {
    "wrangler": "^4.40.2"
  }
}
